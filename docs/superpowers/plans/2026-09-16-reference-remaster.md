# Reference-led visual correction

User rejected the previous remaster as visually unlike both supplied screenshots and explicitly said to implement now. This supersedes the earlier cream/green interpretation. Approval covers the proposed navy/purple illustrated world, yellow CTA, serif editorial headings, compact cards, Studio sidebar/list and mobile bottom navigation. Keep TFG branding and actual API data. Do not copy reference fictional games, metrics or brand.

Implementation sequence: (1) original illustrated world asset + global visual system/Home/public shell, (2) Studio/profile/create/manage and moderation list/disclosure layout, (3) auth and catalog/detail visual hierarchy, (4) mobile bottom navigation, (5) screenshot comparison of every route at desktop/mobile, focused regressions/build/review, (6) update the already-authorized preview server only after verification. Continue in the active turn; scheduled checks are not a substitute for execution.

Preserve auth mutation contracts, ownership, moderation version protection, player iframe identity/sandbox and ENGINE editing logic. Keep working light/dark choice; default visual direction is the illustrated night scene. Explicit light preference is a complementary lavender-dusk version, not the previous cream design. Existing unsupported genres/releases/follower statistics stay absent. Card art uses real covers or honest fallback.

Acceptance: the reference's illustrated backdrop, palette, typography, yellow actions and card treatment must be visible across routes; Studio must have desktop sidebar and compact rows/mobile cards; mobile bottom nav must be real keyboard-accessible links and active-state aware. Visual comparisons and screenshots are required in addition to functional tests.

Review checkpoint: original artwork and reference-led layouts implemented. Focused main unit checks passed 60 tests; creator checks passed 54; review regressions passed 31 (overlapping suites, not an aggregate total). Initial browser run identified login variant selection, default-theme labeling, Home overflow at 1024px and intentional selector/Play-anchor adaptations; corrected before delivery. New desktop login geometry regression failed on the old build (304px off center), confirmed centered on the rebuilt production server.

Final review found no remaining important code issues. Its extra short-viewport check reproduced bottom-dock overlap at 667×390 (player bottom 393.9px, dock top 325.8px); inline player now reserves header/dock space and both 390×667 and 667×390 checks pass. Fullscreen stays independent. Final production build and full lint passed (the pre-existing hierarchy aria-description warning remains). Browser tests are run in batches: a local server received SIGTERM during a long run, so only the interrupted cases are repeated; connection failures are not counted as application passes.

Delivery gate complete: all 56 production-browser cases passed across the completed 20-case segment, 28-case batch and 8-case retry batch. Real PostgreSQL/API ENGINE desktop/mobile acceptance passed 2/2 (28.3s), including real title persistence and hidden site backdrop/dock inside the editor. Temporary stack cleaned itself up. Screenshots inspected for Home, Discover/detail, centered login/split registration, profile, Studio, creation and moderation at desktop/mobile; evidence archived under `/tmp/tfg-reference-final-browser/`, with initial comparison captures under `/tmp/tfg-reference-initial/`. No backend contracts or production data changed by testing. Final deployment follows this verified source commit.
