# Persistent personal game records

User screenshot: /games/ran-san-moi-neon loses record after reload. Verified production CODE game cmtrihgrg0008ns01slxim1rk artifact3 uses bare localStorage (blocked by opaque iframe sandbox), catches error as0; scoresEnabledfalse and no score bridge. Only current CODEgame uses this pattern.

Fix reuses GamePlay/GameScore persistence, no new tables. Add personalBest:number|null to PlaySession and score response, aggregated by gameId AND hashed participantKey. Start returns persisted previous record for that account/browser; saving score returns authoritative personal record. Disabledscoring returnsnull. Existing eligibility/auth/session guards preserved. SQL aggregation inside existingtransaction and no cross-game/user leakage. Keep global highScore response for compatibility.

GamePlayer posts {type:'tfg:score-state',personalBest:number|null} to exact iframe after start and accepted score. Accept {type:'tfg:score-ready'} only from exact iframe and answer when sessionavailable; race beforestart gets initialstate afterstart. Never transmit bearer/sessiontoken. Existing tfg:score remains. Send immediately when changed whenpossible, coalesce concurrent best; flush pending on pagehide/unmount with keepalive to reduce reloadloss. Keep iframe sandbox unchanged. Do not persist per-account scores in shared browserlocalStorage.

Root patches only requested Snake source: replace localStorage load/save with scorebridge, receive bounded personalBest from parent, monotonically update bestdisplay; send on scoreimprovement whileplaying andgameover, so reloadmidgame retains earnedrecord onceacknowledged. Scoreforanewrun remains0; gameplaystate/boardresume is not requested. Enable scoring for thisgame. Use originalsourcebackup and hashcheck; generate a new immutable artifact through existing build/submit/approve lifecycle, do not overwrite publishedartifact bytes or silently bypass moderation. User request authorizes thisspecificgamefix+existingproductiondeployment; don't modify other games/accounts. Backup beforeproductionmutation, log artifactversion and reversible baseline; restore prior source/publication on failure.

API agent owns apps/api/engagement + contracts/test only. Web agent owns GamePlayer + telemetryhelper/tests only. Root owns specificgame patch script/docs/realHTTP+browser+rollout. Independent review after stable. TDD regressions across account/game boundaries, scoremax/startreload, invalidready/source, keepalive. Disposable database and actualpatchedSnakebrowserreload; no fake productiontestscore. Live read-only confirmsgame source/artifact+UI, not fabricated activity.
