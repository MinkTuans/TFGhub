{
  "name": "@indieforge/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit --module ESNext --moduleResolution bundler --target ES2023 --strict src/*.ts",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
